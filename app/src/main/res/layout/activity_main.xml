<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="com.technzone.bai3.data.enums.NavigationTabsEnum"/>
        <variable
            name="viewModel"
            type="com.technzone.bai3.ui.main.viewmodels.MainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <fragment
            android:id="@+id/main_nav_host_fragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/relBvn"
            android:paddingBottom="@dimen/_70sdp"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:navGraph="@navigation/main_nav_graph" />

        <RelativeLayout
            android:id="@+id/relBvn"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_65sdp"
            app:layout_constraintBottom_toBottomOf="parent">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="@dimen/_65sdp"
                android:scaleType="fitXY"
                android:src="@drawable/ic_nav_bg" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="@dimen/_10sdp"
                android:layout_marginTop="@dimen/_minus5sdp"
                android:orientation="horizontal">

                <ImageView
                    android:id="@+id/imgHomeActive"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:src="@{viewModel.selectedTab == NavigationTabsEnum.HOME ? @drawable/ic_active_dot : null}"
                    tools:src="@drawable/ic_active_dot" />

                <ImageView
                    android:id="@+id/imgFavoriteActive"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:src="@{viewModel.selectedTab == NavigationTabsEnum.FAVORITES ? @drawable/ic_active_dot : null}"
                    tools:src="@drawable/ic_active_dot" />

                <ImageView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1" />

                <ImageView
                    android:id="@+id/imgCartActive"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:src="@{viewModel.selectedTab == NavigationTabsEnum.CHART ? @drawable/ic_active_dot : null}"
                    tools:src="@drawable/ic_active_dot" />

                <ImageView
                    android:id="@+id/imgProfileActive"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:src="@{viewModel.selectedTab == NavigationTabsEnum.MORE ? @drawable/ic_active_dot : null}"
                    tools:src="@drawable/ic_active_dot"/>
            </LinearLayout>

            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bnv_main"
                android:layout_width="match_parent"
                android:layout_height="@dimen/_60sdp"
                android:layout_alignParentBottom="true"
                android:background="@android:color/transparent"
                app:elevation="@dimen/dimen_zero"
                app:itemIconSize="@dimen/_18sdp"
                app:itemIconTint="@null"
                app:itemTextAppearanceActive="@style/BottomNavigationView.Active"
                app:itemTextAppearanceInactive="@style/BottomNavigationView"
                app:itemTextColor="@color/color_activity_main_navigation_view_item"
                app:labelVisibilityMode="unlabeled"
                app:menu="@menu/navigation_menu_main" />

        </RelativeLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabBuy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/_45sdp"
            android:src="@drawable/ic_add_product"
            app:backgroundTint="@color/button_color"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:tint="@null" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>